# Next release staging

This feature branch stages the resizable Battery plan/actual/price chart for the next consolidated GW EnergyPilot release.

The manifest, changelog and release index remain on the current released version until field validation is complete. The release consolidation must remove this staging note and update all three version surfaces together.
